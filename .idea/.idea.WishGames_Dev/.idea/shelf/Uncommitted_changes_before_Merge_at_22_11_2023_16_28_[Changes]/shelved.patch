Index: main.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include \"graphical.h\"\r\n#include \"sprite.h\"\r\n#include <iostream>\r\n#include \"DebugUI.h\"\r\n#include \"xinput.h\"\r\n#include \"mapmngr.h\"\r\n\r\nint main()\r\n{\r\n\tGraphical::Initialize(1600, 900);\r\n#ifdef _DEBUG\r\n\tDebugUI::Initialize();\r\n#endif\r\n\tMSG msg;\r\n\tint texNo = LoadTexture(\"player.jpg\");\r\n\tMapMngr map_mngr(\"data/MAP/1.csv\");\r\n\twhile (true) {\r\n\t\t// メッセージ\r\n\t\tif (PeekMessage(&msg, NULL, 0, 0, PM_REMOVE)) {\r\n\t\t\tif (msg.message == WM_QUIT) {\r\n\t\t\t\tbreak;\r\n\t\t\t}\r\n\t\t\telse {\r\n\t\t\t\tTranslateMessage(&msg);\r\n\t\t\t\tDispatchMessage(&msg);\r\n\t\t\t}\r\n\t\t}\r\n\t\telse {\r\n\t\t\tGraphical::Clear(Color(1, 1, 1, 1) * 0.5f);\r\n#ifdef _DEBUG\r\n\t\t\tDebugUI::BeginDraw();\r\n\t\t\t{\r\n\t\t\t\tImGui::Begin(\"Hello, world!\");\r\n\t\t\t\t//ImGui::Text(u8\"テキスト\");\r\n\t\t\t\tif (ImGui::Button(u8\"ボタン\"))\r\n\t\t\t\t{\r\n\t\t\t\t\tstd::cout << \"ボタン押したよ\" << std::endl;\r\n\t\t\t\t}\r\n\r\n\t\t\t\tImGui::End();\r\n\t\t\t}\r\n#endif\r\n\t\t\t//DrawSprite(texNo, Vector2(100, 100), 0, Vector2(100, 100), Color(1, 1, 1));\r\n\t\t\tmap_mngr.Draw();\r\n#ifdef _DEBUG\r\n\t\t\tDebugUI::EndDraw();\r\n#endif\r\n\t\t\tGraphical::Present();\r\n\t\t}\r\n\t}\r\n\r\n\tGraphical::Release();\r\n#ifdef DEBUG\r\n\tDebugUI::Release();\r\n#endif // DEBUG\r\n\r\n\tstd::cout << \"Hello World!\\n\";//基本\r\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/main.cpp b/main.cpp
--- a/main.cpp	(revision 99168dff26cb8e297accc4bb6b2e281a55bc7477)
+++ b/main.cpp	(date 1700636786290)
@@ -5,54 +5,64 @@
 #include "xinput.h"
 #include "mapmngr.h"
 
+bool debug_mode = false;
+
 int main()
 {
-	Graphical::Initialize(1600, 900);
-#ifdef _DEBUG
-	DebugUI::Initialize();
-#endif
-	MSG msg;
-	int texNo = LoadTexture("player.jpg");
-	MapMngr map_mngr("data/MAP/1.csv");
-	while (true) {
-		// メッセージ
-		if (PeekMessage(&msg, NULL, 0, 0, PM_REMOVE)) {
-			if (msg.message == WM_QUIT) {
-				break;
-			}
-			else {
-				TranslateMessage(&msg);
-				DispatchMessage(&msg);
-			}
-		}
-		else {
-			Graphical::Clear(Color(1, 1, 1, 1) * 0.5f);
-#ifdef _DEBUG
-			DebugUI::BeginDraw();
-			{
-				ImGui::Begin("Hello, world!");
-				//ImGui::Text(u8"テキスト");
-				if (ImGui::Button(u8"ボタン"))
-				{
-					std::cout << "ボタン押したよ" << std::endl;
-				}
-
-				ImGui::End();
-			}
-#endif
-			//DrawSprite(texNo, Vector2(100, 100), 0, Vector2(100, 100), Color(1, 1, 1));
-			map_mngr.Draw();
-#ifdef _DEBUG
-			DebugUI::EndDraw();
-#endif
-			Graphical::Present();
-		}
-	}
+    Graphical::Initialize(1600, 900);
+    DebugUI::Initialize();
+    MSG msg;
+    int texNo = LoadTexture("player.jpg");
+    MapMngr map_mngr("data/MAP/1.csv");
+    while (true)
+    {
+        // メッセージ
+        if (PeekMessage(&msg, NULL, 0, 0, PM_REMOVE))
+        {
+            if (msg.message == WM_QUIT)
+            {
+                break;
+            }
+            else
+            {
+                TranslateMessage(&msg);
+                DispatchMessage(&msg);
+            }
+        }
+        else
+        {
+            if (GetKeyState(VK_F1) & 0x8000)
+                debug_mode = !debug_mode;
+            Graphical::Clear(Color(1, 1, 1, 1) * 0.5f);
+            DebugUI::BeginDraw();
+            if (debug_mode)
+            {
+                bool show_demo_window = true;
+                ImGui::ShowDemoWindow(&show_demo_window);
+                ImGuiIO& io = ImGui::GetIO();
+                ImGui::Begin("Debug");
+                ImGui::Text("FPS:%.1f", io.Framerate);
+                ImGui::End();
+                ImGui::Begin("Hello, world!");
+                //ImGui::Text(u8"テキスト");
+                if (ImGui::Button(u8"ボタン"))
+                {
+                    std::cout << "ボタン押したよ" << std::endl;
+                }
+                ImGui::End();
+            }
+
+            //DrawSprite(texNo, Vector2(100, 100), 0, Vector2(100, 100), Color(1, 1, 1));
+            map_mngr.Draw();
+
+            DebugUI::EndDraw();
+            Graphical::Present();
+        }
+    }
 
-	Graphical::Release();
-#ifdef DEBUG
-	DebugUI::Release();
-#endif // DEBUG
+    Graphical::Release();
+    DebugUI::Release();
+
 
-	std::cout << "Hello World!\n";//基本
-}
\ No newline at end of file
+    std::cout << "Hello World!\n"; //基本
+}
Index: imgui.ini
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>[Window][Debug##Default]\r\nPos=60,42\r\nSize=400,400\r\n\r\n[Window][Hello, world!]\r\nPos=455,161\r\nSize=239,152\r\n\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>windows-31j
===================================================================
diff --git a/imgui.ini b/imgui.ini
--- a/imgui.ini	(revision 99168dff26cb8e297accc4bb6b2e281a55bc7477)
+++ b/imgui.ini	(date 1700636807081)
@@ -3,6 +3,14 @@
 Size=400,400
 
 [Window][Hello, world!]
-Pos=455,161
+Pos=57,128
 Size=239,152
 
+[Window][Debug]
+Pos=60,60
+Size=112,63
+
+[Window][Dear ImGui Demo]
+Pos=1002,60
+Size=550,682
+
